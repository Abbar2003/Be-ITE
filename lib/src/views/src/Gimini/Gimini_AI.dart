import 'package:flutter/material.dart';

class GiminiAI extends StatelessWidget {
  const GiminiAI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}